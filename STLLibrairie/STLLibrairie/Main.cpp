#include "Include.hpp"
/*

int main()
{

	//vectors in stl

	vector <int> vec;
	//typedef vector<int>::iterator Myiterator; //declaration d'un type iterator

	// remplissage du vecteur
	vec.push_back(2);
	vec.push_back(4);
	vec.push_back(3);

	Myiterator it1 = vec.begin();  //debut du vecteur
	Myiterator it2 = vec.end();   //fin du vecteur + 1 
	
	
	for (Myiterator it = it1; it < it2; it++)
		cout << *it << "  ";
	cout<<endl;

	sort(it1, it2);

	for (Myiterator it = it1; it < it2; it++)
		cout << *it << "  ";
	cout << endl;

	system("PAUSE");
	return 0;
}
*/